<%= session[:requests] %>

<% @entries.each do |entry| %>
<h1><%= entry.title %></h1>
<p><%= entry.content %></p>
<p><em>Posted at <%= entry.created_at %></em></p>
<% end %>

<%= link_to 'New Entry', new_entry_path %>
<%= link_to 'View All', '/entries/view_all' %>
